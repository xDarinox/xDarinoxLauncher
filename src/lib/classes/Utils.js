let Utils = class {RGBtoHEX(rgbStr) {const [r, g, b] = rgbStr.match(/\d+/g).map(Number);return "#" + ((1 << 24) + (r << 16) + (g << 8) + b).toString(16).slice(1);}fromClassToID(className) {return className.replace(/([A-Z])/g, ' $1').trim().replaceAll(' ', '-').toLowerCase()}codeToString(code) {return code.toString().slice(0, -1)}generateUUID() {return 'xxxxx-xxxxx-xxxx-yxxxx-xxxxx'.replace(/[xy]/g, (c) => {const r = Math.random() * 16 | 0const v = c === 'x' ? r : (r & 0x3 | 0x8)return v.toString(16)})}maskPassword(password) {return password.replace(/./g, '*')}maskEmail(email) {return email.replace(/(^.)(.*)(.@.*$)/, (_, a, b, c) => a + '*'.repeat(Math.min(b.length, 5)) + c)}downloadRewrite(filename, newText) {const element = document.createElement('a');const blob = new Blob([newText], { type: 'text/plain' });element.href = URL.createObjectURL(blob);element.download = filename;document.body.appendChild(element);element.click();document.body.removeChild(element);}async rewriteFile(fileName, newContent) {try {const directoryHandle = await window.showDirectoryPicker();const fileHandle = await directoryHandle.getFileHandle(fileName, { create: true });const writable = await fileHandle.createWritable();await writable.write(newContent);await writable.close();console.log("File rewritten successfully.");} catch (err) {console.error("Error rewriting file:", err);}}async saveFile() {const handle = await window.showSaveFilePicker({suggestedName: 'test.txt',});const writable = await handle.createWritable();await writable.write('Привет, мир!');await writable.close();}}export default new Utils